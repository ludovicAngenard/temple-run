using Newtonsoft.Json.Linq;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DarkDiamond : Gem
{
    // Start is called before the first frame update
    void Start()
    {
        _value = 50;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
